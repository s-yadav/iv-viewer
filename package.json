{
  "name": "iv-viewer",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "storybook": "yarn workspace react-iv-viewer storybook",
    "build-react": "yarn workspace react-iv-viewer build",
    "build-core": "yarn workspace iv-viewer bundle",
    "start-core": "yarn workspace iv-viewer start",
    "build": "yarn build-core && yarn build-react"
  },
  "devDependencies": {
    "all-contributors-cli": "^6.26.1"
  }
}
