{
  "name": "img-viewer",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "storybook": "yarn workspace react-img-viewer storybook",
    "build-react": "yarn workspace react-img-viewer build",
    "build-core": "yarn workspace iv-viewer build",
    "start-core": "yarn workspace iv-viewer start"
  }
}